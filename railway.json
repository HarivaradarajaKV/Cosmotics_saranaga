{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "npm config set python /usr/bin/python3 && PYTHON=/usr/bin/python3 npm install --build-from-source && PYTHON=/usr/bin/python3 npm rebuild bcrypt --build-from-source"
    },
    "deploy": {
        "startCommand": "npm start",
        "healthcheckPath": "/health",
        "healthcheckTimeout": 300,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
} 